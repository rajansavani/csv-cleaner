# csv-cleaner
CSVCleaner is a small Python service that takes a messy CSV and returns a cleaned version plus a clear report of what changed. It uses OpenAI to propose a safe cleaning plan, then applies the steps with pandas. Includes a REST API, CLI-style demo scripts, and tests.
